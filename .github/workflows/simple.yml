
name : Shell commands

on : [push]

jobs : 
  run-shell-command:
    runs-on : ubuntu-latest
    steps:
      -  name : echo a string 
         run : echo "Hello world"

      - name: multiline script
        run : |
          node -v
          npm -v
      - name : python command
        run : |
          import platfrom
          print
          (platfrom.processor())
        shell: python
=======
name : Shell comands

on : [push]

jobs : 
  run-shell-command:
    runs-on : ubuntu-latest
    steps:
      -  name : echo a string 
         run : echo "Hello world"

      - name: multiline script
        run : |
          node -v
          npm -v
      - name : python command
        run : |
          import platform
          print
          (platform.processor())
        shell: python
  run-windows-command :
    runs-on : windows-latest
    needs : ["run-shell-command"]
    steps:
      - name: Directory powershell
        run: Get-location
      - name: Directory bash
        run : pwd
        shell:  bash
>>>>>>> 102590a8a906af02487037acbd80efd7bb1c9b87
